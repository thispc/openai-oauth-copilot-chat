{
  "name": "openai-oauth-copilot-chat",
  "displayName": "Codex Bridge for Copilot Chat",
  "description": "Use OpenAI Codex models in GitHub Copilot Chat with your ChatGPT Plus or Pro subscription.",
  "version": "0.11.1",
  "publisher": "grikomsn",
  "license": "MIT",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/grikomsn/openai-oauth-copilot-chat.git"
  },
  "bugs": {
    "url": "https://github.com/grikomsn/openai-oauth-copilot-chat/issues"
  },
  "homepage": "https://github.com/grikomsn/openai-oauth-copilot-chat#readme",
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Machine Learning"
  ],
  "keywords": [
    "openai",
    "codex",
    "chatgpt",
    "copilot",
    "oauth",
    "language-model"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onLanguageModelChatProvider:openai-codex"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "openaiCodex.manage",
        "title": "Codex Bridge: Manage Connection"
      },
      {
        "command": "openaiCodex.addAccount",
        "title": "Codex Bridge: Add ChatGPT Account"
      },
      {
        "command": "openaiCodex.selectProfile",
        "title": "Codex Bridge: Select Profile for Usage and Management"
      },
      {
        "command": "openaiCodex.signIn",
        "title": "Codex Bridge: Sign In with ChatGPT"
      },
      {
        "command": "openaiCodex.signInManual",
        "title": "Codex Bridge: Sign In Manually"
      },
      {
        "command": "openaiCodex.importCodexSession",
        "title": "Codex Bridge: Import Codex CLI Session (Advanced)"
      },
      {
        "command": "openaiCodex.testConnection",
        "title": "Codex Bridge: Test Connection"
      },
      {
        "command": "openaiCodex.refreshModels",
        "title": "Codex Bridge: Refresh Models"
      },
      {
        "command": "openaiCodex.showUsage",
        "title": "Codex Bridge: Show Usage"
      },
      {
        "command": "openaiCodex.diagnostics",
        "title": "Codex Bridge: Show Diagnostics"
      },
      {
        "command": "openaiCodex.showModelSelection",
        "title": "Codex Bridge: Show Effective Model"
      }
    ],
    "configuration": {
      "title": "Codex Bridge for Copilot Chat",
      "properties": {
        "openaiCodex.codexVersion": {
          "type": "string",
          "default": "",
          "pattern": "^(?:\\d+\\.\\d+\\.\\d+(?:-[0-9A-Za-z.-]+)?)?$",
          "description": "Optional Codex client version for the live model catalog. Leave empty to use the extension's checked-in version."
        },
        "openaiCodex.reasoningEffort": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultra"
          ],
          "default": "low",
          "deprecationMessage": "Legacy workspace fallback retained for compatibility; prefer the live model picker.",
          "description": "Legacy default reasoning effort sent to Codex when no model-specific value is selected. Unsupported values fall back to the live model default."
        },
        "openaiCodex.speedMode": {
          "type": "string",
          "enum": [
            "normal",
            "fast"
          ],
          "default": "normal",
          "deprecationMessage": "Legacy workspace fallback retained for compatibility; prefer the per-model Speed Mode control.",
          "enumDescriptions": [
            "Standard speed and usage",
            "1.5x speed with increased usage on supported models"
          ],
          "description": "Legacy default Codex processing speed. The live catalog and model-specific Speed Mode control remain authoritative."
        },
        "openaiCodex.reasoningSummary": {
          "type": "string",
          "enum": [
            "model",
            "auto",
            "concise",
            "detailed",
            "none"
          ],
          "default": "auto",
          "enumDescriptions": [
            "Use the default advertised by the live model catalog",
            "Let Codex choose the summary detail",
            "Return a concise reasoning summary",
            "Return a detailed reasoning summary",
            "Do not request a reasoning summary"
          ],
          "description": "Default reasoning summary detail sent to Codex. A model-picker selection overrides this value; models that do not accept the reasoning.summary parameter ignore it."
        },
        "openaiCodex.requestTimeoutSeconds": {
          "type": "number",
          "default": 600,
          "minimum": 10,
          "description": "Maximum duration of a Codex request in seconds."
        },
        "openaiCodex.catalogCacheMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "How long the live Codex model catalog is reused before refreshing."
        },
        "openaiCodex.debugLogging": {
          "type": "boolean",
          "default": false,
          "description": "Log request and stream metadata. Prompts and OAuth tokens are never logged."
        },
        "openaiCodex.showUsageStatusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show ChatGPT Codex quota utilization and inference token usage in the status bar."
        },
        "openaiCodex.modelSelection": {
          "type": "object",
          "default": {
            "preferredModelId": "",
            "fallbackModelIds": []
          },
          "properties": {
            "preferredModelId": {
              "type": "string",
              "default": "",
              "description": "Live Codex model ID to prefer when Copilot requests Auto or when this model is selected."
            },
            "fallbackModelIds": {
              "type": "array",
              "items": {
                "type": "string",
                "pattern": "^[A-Za-z0-9._:-]+$"
              },
              "default": [],
              "description": "Ordered live Codex model IDs to try after the preferred model is rate-limited or unavailable."
            }
          },
          "description": "Optional live model pin and ordered quota/rate-limit fallbacks. Explicit picker selections remain authoritative."
        }
      }
    },
    "languageModelChatProviders": [
      {
        "vendor": "openai-codex",
        "displayName": "Codex Bridge",
        "configuration": {
          "type": "object",
          "properties": {
            "profile": {
              "type": "string",
              "title": "Codex Bridge Profile",
              "description": "Profile ID created with Codex Bridge: Add ChatGPT Account (for example, personal or work).",
              "default": "default",
              "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$"
            }
          }
        }
      }
    ]
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('out',{recursive:true,force:true})\"",
    "compile": "npm run clean && tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node --test \"out/**/*.test.js\"",
    "check": "npm test",
    "changeset": "changeset",
    "update-codex-version": "node scripts/update-codex-version.mjs",
    "version": "changeset version",
    "release": "npm run check && vsce publish --skip-duplicate",
    "prepackage": "npm run check",
    "package": "vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.125.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^6.0.3"
  }
}
